#include<iostream>
using namespace std;
main(){
    double sum=0,x,y;
    cout<<"Enter x: ";
    cin>>x;
    while (x!=0){
   if(x>0){
       sum=sum+x;
       }
       cout<<"Enter x: ";
        cin>>x;
   }
       cout<<"sum = "<<sum;
   }
   
